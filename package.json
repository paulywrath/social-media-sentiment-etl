{
  "name": "social-media-sentiment-etl",
  "version": "1.0.0",
  "description": "A lightweight ETL pipeline that fetches posts from r/marcmaron, stores post data in PostgreSQL, transforms the data, and exposes results.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "pg": "^8.16.2"
  }
}
